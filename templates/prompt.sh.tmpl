# shellcheck shell=bash
# Appended to /etc/bash.bashrc, so it is a fragment with no shebang of its own.
if [ -n "$VSWARM_USER" ]; then
  PS1='\[\e[1;32m\]'"$VSWARM_USER"'@\h\[\e[0m\]:\[\e[1;34m\]\w\[\e[0m\]\$ '
fi
